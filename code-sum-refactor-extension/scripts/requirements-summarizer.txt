# Clone Visualizer Python dependencies.
# Install into the same interpreter as "Clone Visualizer: Python Path", e.g.:
#   python3 -m pip install -r scripts/requirements-summarizer.txt

# ── Code summarization (Dreamuno HF models) ──────────────────────────────────
protobuf>=4.21.0
transformers>=4.36.0
torch>=2.0.0
sentencepiece>=0.1.99

# ── Tree-sitter refactoring ───────────────────────────────────────────────────
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0

# ── REST server (FastAPI + uvicorn) ──────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
